# SQL 模板库（直接复制，替换条件即可）

> 本文档与站点 SQL 校验器规则一致。AI 生成模板时优先复用本文件中的成熟写法；改阈值、改文案后仍要在本地按 `api-reference.md` 第 4 节自检，再通过 API 写入。

> **条件分层原则**：规则落在“平均水平”→ 用条件句组（均值分档）；规则落在“任一天 / 任一行”（如任一天完课时长<100 即算未完成）→ 必须用本文件的 SQL 写法（0 行隐藏机制），均值分档会漏判/误判。站内 SQL 是 alaSQL 在浏览器内存里对 Excel 行执行的 SQL 式查询，不是标准 SQL。

## 状态判断类（单值返回，用于「课程：已完成√」这种行）

### 课程状态：任一天完课时长<100 或为空 → 未完成
SELECT CASE WHEN MIN(完课时长) >= 100 AND COUNT(完课时长) = COUNT(*) THEN '已完成√' ELSE '未完成×' END AS 状态 FROM 全局数据

### 出门测状态：任一天出门测正确率为空 → 未完成
SELECT CASE WHEN COUNT(出门测正确率) = COUNT(*) THEN '已完成√' ELSE '未完成×' END AS 状态 FROM 全局数据

## 条件行类（全部满足才输出文案，否则 0 行 → 整行隐藏）

### 全部完成才显示鼓励句（GROUP BY 学生ID 仅为满足校验，语义=全部一组）
SELECT '在众多同学中你已经脱颖而出了，未来继续保持，争取永不缺席！' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(完课时长) = COUNT(*) AND MIN(完课时长) >= 100 AND COUNT(出门测正确率) = COUNT(*)

## 明细列表类（列知识点/课次）

### 出门测正确率最低的 3 个知识点（不含未完成的）
SELECT 科目, 课次讲数, 课次名称, 出门测正确率 FROM 全局数据 WHERE 出门测正确率 IS NOT NULL AND 出门测正确率 < 80 ORDER BY 出门测正确率 ASC, 课次讲数 ASC LIMIT 3

### 设置了但未完成的出门测
SELECT 科目, 课次讲数 FROM 全局数据 WHERE 出门测总分 IS NOT NULL AND 出门测正确率 IS NULL ORDER BY 课次讲数 ASC

### 完课时长不足 100 分钟的讲次明细（任一天语义：列出具体哪几讲）
SELECT 课次讲数, 课次名称, 完课时长 FROM 全局数据 WHERE 完课时长 < 100 ORDER BY 课次讲数 ASC

### 按科目算综合分取前 2
SELECT 科目, ROUND(AVG(出门测正确率) * 0.6 + AVG(正确率) * 0.4, 2) AS 综合分 FROM 全局数据 GROUP BY 科目 HAVING COUNT(出门测正确率) > 0 AND COUNT(正确率) > 0 ORDER BY 综合分 DESC LIMIT 2

## 成套模板：出勤 + 出门测（6 条，任一天语义，2026-08 已实测）

规则：任一天完课时长<100 或有「-」→ 出勤有未完成；出门测任一天缺测 → 未完成；全部完成时按平均正确率分档（≥80 亮眼 / 60~79 提升空间 / <60 需巩固）。出门测正确率在 SQL 中为 0~100 数值。

用法：每个查询返回 1 行 1 字段「内容」，**取消「自动编号」**；块话术里每行放一个【查询名.内容】引用，查询 0 行 → 该行自动隐藏，两个维度各命中且仅命中一句。

- 出勤完成：SELECT '这段时间每一讲都按时出勤、完整听完，出勤习惯满分，继续保持！' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(完课时长) = COUNT(*) AND MIN(完课时长) >= 100
- 出勤未完成：SELECT '有几讲没能完整听完，记得抽空补看回放，把知识点补上哦。' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(完课时长) < COUNT(*) OR MIN(完课时长) < 100
- 出门测未完成：SELECT '还有出门测没有完成，记得及时补测哦。' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(出门测正确率) < COUNT(*)
- 出门测优秀：SELECT '出门测也都认真完成了，成绩很亮眼！' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(出门测正确率) = COUNT(*) AND AVG(出门测正确率) >= 80
- 出门测一般：SELECT '出门测都完成了，正确率还有提升空间，继续加油！' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(出门测正确率) = COUNT(*) AND AVG(出门测正确率) >= 60 AND AVG(出门测正确率) < 80
- 出门测需加强：SELECT '出门测都完成了，不过正确率还不理想，建议结合错题多巩固哦。' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(出门测正确率) = COUNT(*) AND AVG(出门测正确率) < 60

> 站点版本说明：线上站已部署“服务端校验跳过 SQL 执行”的修复，本文件中的 SQL 模板均可通过 API 直接保存。若某条模板 400，先按 `api-reference.md` 第 4 节逐条核对 SQL 校验规则，再把报错原文保留下来分析。

## 成套模板：阶段综合反馈（四段式，10 条查询，2026-08 已上线实测）

结构：开头固定句 + 📚出勤 / 💡课堂参与 / 📝课后练习 / ✨总结 四个固定句块（块间「直接换行」）。每个动态片段独占一行，靠 0 行隐藏机制独立显隐；列表查询关闭「自动编号」，按需设分隔符（顿号 / 自定义“和”）。

| 块 | 查询 | SQL |
|---|---|---|
| 出勤 | 出勤完成 | SELECT '📚 出勤表现：孩子在本阶段所有课次完课时长均超过 100 分钟，全程稳定出勤，学习态度踏实专注，课堂参与度良好。' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(完课时长) = COUNT(*) AND MIN(完课时长) >= 100 |
| 出勤 | 出勤不足（列最多3个，分隔符顿号）| SELECT 科目, 课次讲数 FROM 全局数据 WHERE 完课时长 < 100 OR 完课时长 IS NULL ORDER BY 课次讲数 ASC LIMIT 3 |
| 课堂参与 | 突出（最高2讲，分隔符自定义“和”）| SELECT 科目, 课次讲数, 正确率 FROM 全局数据 WHERE 正确率 IS NOT NULL ORDER BY 正确率 DESC, 课次讲数 ASC LIMIT 2 |
| 课堂参与 | 偏低（<50%最低2讲，分隔符顿号）| SELECT 科目, 课次讲数 FROM 全局数据 WHERE 正确率 IS NOT NULL AND 正确率 < 50 ORDER BY 正确率 ASC, 课次讲数 ASC LIMIT 2 |
| 课堂参与 | 无答题数据兜底 | SELECT '💡 课堂参与：本阶段课堂答题数据较少，暂不评估。' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(正确率) = 0 |
| 课后练习 | 课后全完成 | SELECT '📝 课后练习：课后出门测已全部完成，基础知识点掌握扎实。' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(出门测正确率) = COUNT(*) |
| 课后练习 | 缺测（列最多3个，分隔符顿号）| SELECT 科目, 课次讲数 FROM 全局数据 WHERE 出门测正确率 IS NULL ORDER BY 课次讲数 ASC LIMIT 3 |
| 课后练习 | 满分科目（同科目≥2次）| SELECT 科目 FROM 全局数据 WHERE 出门测正确率 >= 100 GROUP BY 科目 HAVING COUNT(*) >= 2 ORDER BY 科目 |
| 总结 | 总结全勤（空串开关技巧）| SELECT ' ' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(完课时长) = COUNT(*) AND MIN(完课时长) >= 100 |
| 总结 | 总结非全勤 | SELECT ' ' AS 内容 FROM 全局数据 GROUP BY 学生ID HAVING COUNT(完课时长) < COUNT(*) OR MIN(完课时长) < 100 |

话术要点（已实测）：
- 出勤块两行：`【出勤完成.内容】` / `📚 出勤表现：本阶段以下课次完课时长不足 100 分钟：{出勤不足.科目1~3第出勤不足.课次讲数1~3讲}，建议抽空补看回放，把缺的内容补上。`
- 课堂参与三行：`💡 课堂参与：从答题数据来看，{突出.科目1~2第突出.课次讲数1~2讲（正确率 突出.正确率1~2）}表现突出，知识吸收效果较好。` / `{偏低.科目1~2第偏低.课次讲数1~2讲}等科目正确率偏低，建议后续利用课程回放针对性梳理错题、强化薄弱知识点。` / `【无答题数据.内容】`
- 课后练习三行：`【课后全完成.内容】` / `📝 课后练习：{缺测.科目1~3第缺测.课次讲数1~3讲}的出门测尚未完成，烦请您提醒孩子及时补做，以便我们完整掌握他的学习情况。` / `同时，{满分科目.科目}多次取得满分，基础知识点掌握扎实。`
- 总结两行（ykt 必须写在话术里，不能写进 SQL 常量——SQL 结果不做 ykt 替换）：`✨ 学习总结：ykt整体学习状态积极，全勤表现值得肯定，期待他在后续学习中继续保持专注，稳步提升成绩💪{总结全勤.内容}` / `✨ 学习总结：ykt整体学习状态积极，个别课次还需补看回放，期待他后续跟上节奏、稳步提升成绩💪{总结非全勤.内容}`
- 「正确率」字段引用自动带 %（别在话术里再写 %）；2 位小数显示需站点 formatNumber 为 toFixed(2)（2026-08 已改并部署）。
- 偏低阈值取 <50% 而非 <80%：与“突出=最高2讲（无阈值）”数学上永不重叠；80% 会让 2~3 讲的学生同一讲既被夸又被批。

## 话术引用写法

- 单字段单行：课程：{课程状态.状态}
- 第 1 条：{查询名.字段名1}
- 第 1~3 条：{查询名.字段名1~3}
- 同行多字段循环：{低分课次.科目1~3第低分课次.课次讲数1~3讲：低分课次.课次名称1~3}
- 查询 0 行 → 引用该查询的整行隐藏（实现条件句）
- 学生称呼占位符：ykt（不带花括号）